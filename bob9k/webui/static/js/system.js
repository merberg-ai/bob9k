document.addEventListener('DOMContentLoaded',async()=>{if(document.body.dataset.page!=='system')return;try{const data=await window.bob9kApi.getSystem();document.getElementById('system-ip').textContent=`IP: ${data.ip}`;document.getElementById('system-mode').textContent=`Mode: ${data.mode}`;document.getElementById('system-led').textContent=`LED state: ${data.led_state}`;}catch(err){}});
